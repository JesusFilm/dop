[build]
builder = "NIXPACKS"

[deploy]
preDeployCommand = "pnpm exec prisma migrate deploy && pnpm db:seed:production"
startCommand = "pnpm start"
healthcheckPath = "/api/health"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
